const express = require("express");
const { validation } = require("../constant/validate");
const adminAuth = require("../middleware/adminAuth");
const {  } = require("../controller/stock");

const router = express.Router();



module.exports = router;
